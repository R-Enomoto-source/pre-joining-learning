抽象クラスは {{c1::`abstract`}} が付いたクラスで、「そのクラス単体では不完全で、必ずサブクラスを作って使う前提」のクラスである。	java 第5章 抽象クラス
抽象クラスは {{c1::`new` で直接インスタンス化できない}}。	java 第5章 抽象クラス
抽象メソッドは、本体（`{}` の中身）がないメソッドで、「ここにこういう名前・引数・戻り値のメソッドがあるべきだ」という {{c1::約束だけを書いたもの}} である。	java 第5章 抽象メソッド
抽象メソッドを含む抽象クラスを使うとき、実際の処理は {{c1::サブクラス}} が必ず実装する。	java 第5章 抽象メソッド
`AbstractSample s = new ConcreteSample(); s.sample();` の実行では、`sample()` 内で呼ばれる `test()` の実装は {{c1::`ConcreteSample` の `test()`}} が使われる。	java 第5章 抽象クラス
`AbstractSample` の `sample()` から `ConcreteSample.test()` を呼ぶ実行結果は、表示順が {{c1::"A"}} → {{c2::"B"}} → {{c3::"C"}} となる。	java 第5章 抽象クラス
オブジェクト指向では、「変数の型」と「実際のインスタンスの型」を区別して考える必要があり、`A a = new B();` の場合、変数の型は {{c1::A}}、実際のインスタンスの型は {{c2::B}} となる。	java 第5章 ポリモーフィズム
メソッド呼び出しでは、実際のインスタンスの型（オーバーライドした方）が使われ、フィールド参照では {{c1::変数の型}} のフィールドが使われる。	java 第5章 ポリモーフィズム
親子クラスで同名フィールドを持つ場合、`A b = new B(); System.out.print(b.val);` は、{{c1::変数の型 A 側の `val`}} を参照する。	java 第5章 ポリモーフィズム
`Parent` と `Child` がそれぞれ `name` フィールドを持つ例では、`child.name = "sample";` によって値が入るのは {{c1::Child 側の `name`}} であり、`child.getName()` が参照するのは {{c2::Parent 側の `name`}} なので、出力は {{c3::null}} になる。	java 第5章 ポリモーフィズム
インタフェースのメソッド宣言は、何も書かなくても自動的に {{c1::`public abstract`}} となる。	java 第5章 インタフェース
インタフェースには、抽象メソッド、{{c1::default メソッド}}、{{c2::static メソッド}} を書くことができる。	java 第5章 インタフェース
default メソッドは、インタフェースに {{c1::標準的な実装}} を持たせたいときに使う。	java 第5章 インタフェース
インタフェースで `Object` 由来のメソッド（例: `toString`）を default メソッドとしてオーバーライドしようとすると、{{c1::インタフェース側でコンパイルエラー}} になる。	java 第5章 インタフェース
`A.super.sample()` 構文は、インタフェース A を {{c1::直接継承しているインタフェース}} または {{c2::直接 implements しているクラス}} からだけ使える。	java 第5章 インタフェース
インタフェースの private メソッドの目的は、{{c1::default メソッドや static メソッドの共通処理をまとめること}} であり、private メソッドには必ず {{c2::本体 `{}`}} が必要である。	java 第5章 インタフェース
インタフェースの抽象メソッドを {{c1::`private`}} にすることはできない。	java 第5章 インタフェース
インタフェースは {{c1::`new` できない}} ため、`new A()` のようなコードは {{c2::コンパイルエラー}} になる。	java 第5章 インタフェース
配列の要素型と実際に入れるインスタンスの型の間には、「{{c1::代入可能な関係}}」が必要である。	java 第5章 代入互換性
`A[] array = { new B(), new C(), new A(), new D() };` の例で、`new A()` がコンパイルエラーになる理由は、{{c1::インタフェース A を `new` しようとしている}} からであり、`new D()` がコンパイルエラーになる理由は {{c2::D が A とまったく無関係な型だから}} である。	java 第5章 代入互換性
アップキャストは {{c1::子クラス → 親クラス}} へのキャストであり、通常はキャスト演算子なしで {{c2::自動的に OK}} となる。	java 第5章 キャスト
ダウンキャストは {{c1::親クラス → 子クラス}} へのキャストであり、{{c2::キャスト演算子が必要}} になる。	java 第5章 キャスト
`A a = new B(); B b = (B) a;` のようなダウンキャストでは、実際のインスタンスが B なので {{c1::実行時にも正常に動作}} する。	java 第5章 キャスト
`A a = new A(); B b = (B) a;` のようなキャストは、コンパイル時には {{c1::A と B の間に継承関係がある}} ため一応 OK とみなされるが、実行時には {{c2::`ClassCastException` が発生}} する。	java 第5章 キャスト
キャスト式を書くと、「コンパイル時の型チェック」は通るが、{{c1::実際のインスタンスが互換性を持たないと実行時に例外になる}}。	java 第5章 キャスト
sealed クラスは、「{{c1::誰が継承してよいかを親側で限定する仕組み}}」であり、`public sealed class Sample permits A, B {}` は {{c2::Sample を継承できるクラスを A と B に限定する}} という意味になる。	java 第5章 sealed
sealed クラスを継承するクラスは、必ず {{c1::`final`}} / {{c2::`sealed`}} / {{c3::`non-sealed`}} のいずれかで修飾しなければならない。	java 第5章 sealed
sealed インタフェースでも、親から直接実装するクラス／インタフェースには {{c1::`final` / `sealed` / `non-sealed` のいずれかの修飾子}} が必須であり、何も付けないと {{c2::コンパイルエラー}} になる。	java 第5章 sealed
オーバーライドでは、メソッド名・引数の型・数・順番が同じである必要があり、これを {{c1::シグニチャが同じ}} という。	java 第5章 オーバーライド
オーバーライドで戻り値型を変える場合、元と同じか、その {{c1::サブクラス型（共変戻り値）}} にだけ変更できる。	java 第5章 オーバーライド
オーバーライドでアクセス修飾子を変更する場合、同じか、より {{c1::“緩い”（公開範囲の広い）}} 方向への変更のみ許される。	java 第5章 オーバーライド
親クラスと子クラスのコンストラクタ呼び出し順序は、{{c1::親クラスのコンストラクタが先に動き、子クラスのコンストラクタが後で動く}}。	java 第5章 コンストラクタ
子クラスのコンストラクタの先頭には、暗黙に {{c1::`super();`}} が挿入される。	java 第5章 コンストラクタ
同じクラス内の別コンストラクタを呼びたいときは、{{c1::`this(...)`}} を使う。	java 第5章 コンストラクタ
コンストラクタでフィールドと引数が同名のとき、フィールドを指すには {{c1::`this.フィールド名`}} を使う。	java 第5章 コンストラクタ
