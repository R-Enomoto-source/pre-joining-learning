書き方
01. ①答え
    ②答えの導き方
    ③どういうことが分かっていないのか、どの点が理解不足かを書く
1.  A
    4,5行目でエラーも例外が発生しないので、finishと表示されるのではないか。
    配列の知識が曖昧で、どうしたらエラーが起るのかが理解しきれていない。
2.  C
    String型なので、何も渡さない場合はnullになる。そうすると、catch文に移行して、処理を実行してBを表示して、finallyは最後に必ず実行するのでCを表示。B、Cを表示する。
3.  A
    例外クラスに何も記述していないため、どんな例外かが分からないので、処理の仕様がないから。と思ったが、別に何も書いていなくとも、例外を継承しているので、問題なくtrycatch構文は機能するのではないか。なので、ABと表示されるのではないか。と思ったが選択肢にない。とすると、例外を直接継承していない、subが機能しないのではないか。よってAだけが表示されるのではないか。答えはA。
4.  B
    5行目でnullが入っているので、例外が発生して、catch→finallyと処理するためCBと表示される。
5.  C
    nullが入っているので例外が発生して、catchでAを返して、Aが表示されて、finallyが実行されてBが表示されるため。returnで数値化を返すところまでで、finallyに処理が移行するので、まずBと表示されて、その後mainに戻って、test(A)が実行されて、Aと表示されるのでは
    catchのなかでreturnがどう処理されるのかが分からないので自信をもって回答できていない。
6.  B
    10が戻されるが、finallyで20が戻されて上書きされるため。こうならなければコンパイルエラー。
7.  E
    10行目で存在しないインデックスを指定しているが、catchしている例外がこれで発生する例外ではないため。
8.  D
    catchがない、または、finallyが2つ以上あるためコンパイルエラーになるのではないか？
    catchがない場合、finallyが2つ以上ある場合にどういう挙動になるのかが分かっていない。
9.  B
    6行目で例外が発生して、catch→finally→finallyという流れで処理されるため。12行目のcatchに行かないのは、3行目のtryでは例外が発生しないため。ネストされたtryの中で処理が完了しているためではないか。
10. B
    例外処理が必須であるクラスが、Exceptionクラスのため。
11. E
    ここに書いても意味はないため。ここに書くのは、メソッドが呼び出される場合だが、呼び出す他のメソッドがないので、書く意味がない。
12. D
    try-catch文があるので、間違っているため。
13. C
    何も入力されていないので、args[0]は存在しない。よって、nullが入っているということもないため、Cの例外が発生する。
14. D
    listに関係するエラーDだけであるように思えるため。
15. A
    順番に処理していった結果、aの10とbの10を比較したとき、trueになるため。
16. B
    ""とnullは区別されるため。
17. A
    CかDで山勘でどちらいか。と思ったが、これがどういうエラーになるのかが分からない。自分を呼んでいるので、無限ループになりそう。そうするとAが答えになる。
    何故無限ループになりそうなのかの明確な理由は分からない。
18. A
    複数のエラーを受ける方法として、正しいのが|一つであるため。
19. C
    何となく。マルチキャッチについてのルールを学習しなかったため、そもそもないのかと思ったため。
20. E
    catchブロックがないと、例外が発生した時の処理が記述できないため。
    他の問題を見るとcatchブロックは必須ではなさそうであるため。ただないのにtryブロックを書く意味はなさそう。
21. B
    確かそうだった気がする。これは完全に知識問題。
22. AD
    それ以外はスコープ範囲等の問題でエラーになるから。
23. A
    try()の中での順で処理されると考えるとこうなるため。
24. D
    まずcatchブロックを実行しないと、例外処理が記述および実行できない。そして、finallyブロックが先に来てしまうと、clozeしていなかった場合別のエラーが発生してしまうため、確実にclozeさせるためにclozeブロックがfinallyブロックより先に来るべき
25. 
26. 